export interface ProductListData {
  id: string;
  productTitle: string;
  productPrice: number;
  productCurrency: number;
  productImagePath: string;
  retailerName: string;
  retailerUrl: string;
}
